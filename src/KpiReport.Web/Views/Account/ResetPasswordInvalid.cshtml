@{
    ViewBag.Title = "ลิงก์ใช้ไม่ได้";
}

<div class="kpi-auth">
    <div class="kpi-panel" style="text-align:center;">
        <h2>ลิงก์นี้ใช้ไม่ได้แล้ว</h2>
        <p class="kpi-subtitle" style="margin:10px 0 18px;">
            ลิงก์ตั้งรหัสผ่านใหม่มีอายุ 1 ชั่วโมง และใช้ได้เพียงครั้งเดียว
            ลิงก์นี้อาจหมดอายุ ถูกใช้ไปแล้ว หรือคัดลอกมาไม่ครบ
        </p>
        <a class="btn-kpi" href="@Url.Action("ForgotPassword", "Account")">ขอลิงก์ใหม่</a>
    </div>
</div>
